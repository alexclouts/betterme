[project]
name = "betterme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexandre", email = "331047197+alexclouts@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
betterme = "betterme:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
